spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/somuuat
    username: somu
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true
